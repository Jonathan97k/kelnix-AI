const fs = require('fs'); const path = 'src/pages/Editor.tsx'; const content = fs.readFileSync(path, 'utf8').split('\n'); const truncated = content.slice(0, 537).join('\n'); const footer = '      <MediaPicker\n        isOpen={isMediaPickerOpen}\n        onClose={() => setIsMediaPickerOpen(false)}\n        onSelect={handleMediaSelect}\n      />\n    </div>\n  );\n};\n\nexport default Editor;'; fs.writeFileSync(path, truncated + '\n' + footer);
